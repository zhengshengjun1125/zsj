<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zsj.system.dao.MenuDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.zsj.system.entity.MenuEntity" id="menuMap">
        <result property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="title" column="title"/>
        <result property="component" column="component"/>
        <result property="sortValue" column="sort_value"/>
        <result property="status" column="status"/>
        <result property="hasChildren" column="has_children"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="isDeleted" column="is_deleted"/>
    </resultMap>

    <resultMap id="sysMenuMap" type="com.zsj.system.entity.MenuEntity" autoMapping="true"></resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,parent_id,title,component,sort_value,status,create_time,update_time,is_deleted
    </sql>

    <select id="selectAll" resultMap="sysMenuMap">
        select <include refid="columns" />
        from sys_menu
        where
        is_deleted = 0
        order by sort_value
    </select>



    <update id="updateById" >
        update sys_menu set
        <if test="parentId != null and parentId != ''">
            parent_id = #{parentId},
        </if>
        <if test="title != null and title != ''">
            title = #{title},
        </if>
        <if test="component != null and component != ''">
            component = #{component},
        </if>
        <if test="sortValue != null">
            sort_value = #{sortValue},
        </if>
        <if test="status != null">
            status = #{status},
        </if>
        update_time =  now()
        where
        id = #{id}
    </update>


    <select id="selectListByUserId" resultMap="sysMenuMap">
        SELECT DISTINCT m.* FROM sys_menu m
                                     INNER JOIN sys_role_menu rm ON rm.menu_id = m.id
                                     INNER JOIN sys_role ur ON ur.id = rm.role_id
        WHERE ur.id=#{userId} and m.is_deleted = 0
    </select>


<!--    <insert id="insert" useGeneratedKeys="true" keyProperty="id">-->
<!--        insert into sys_menu (-->
<!--            id,-->
<!--            parent_id,-->
<!--            title,-->
<!--            component,-->
<!--            sort_value,-->
<!--            status-->
<!--        ) values (-->
<!--                     #{id},-->
<!--                     #{parentId},-->
<!--                     #{title},-->
<!--                     #{component},-->
<!--                     #{sortValue},-->
<!--                     #{status}-->
<!--                 )-->
<!--    </insert>-->
</mapper>