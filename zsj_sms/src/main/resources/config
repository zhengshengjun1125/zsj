server:
  port: 11111

spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/zsj_sms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: zsj20001125
  cloud:
    util:
      enabled: false
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        import-check:
          enabled: false
  rabbitmq:
    host: 127.0.0.1
    username: zsj
    password: zsj20001125
    port: 5672
    virtual-host: /
    #开启发送端发布确认  并且启用回调方法
    publisher-confirm-type: correlated
    #开启发送端消息抵达队列的确认
    publisher-returns: true
    # 只要抵达队列,以异步模式优先回调return confirm
    template:
      mandatory: true
    listener:
      simple:
        #开始手动应答模式  简单模式
        acknowledge-mode: manual


  application:
    name: zsjSms

  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    timeout: 5000
    lettuce:
      pool:
        max-active: 32
        max-wait: -1
        max-idle: 16
        min-idle: 8


mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl



